import { Router } from 'express'
// import { CreateCustomerController } from '../modules/create-client.controller'

const router = Router()

router.post('/pedidos', (req, res) => {
	// new CreateCustomerController().handle(req, res)
})

export { router }
